{
  // Production dependency audit gate — see .github/workflows/production-deployment.yml
  //
  // allowlist: GHSA-qwww-vcr4-c8h2 ("React Router: RSC Mode CSRF Bypass Allows
  // Action Execution Before 400 Response", react-router / react-router-dom).
  //
  // Confirmed NOT applicable to this app (investigated 2026-07-27, react-router-dom
  // 6.30.4 -> 7.18.1 upgrade): grepped all source for React Server Components /
  // "react-server" usage (none found), confirmed vite.config.js has no SSR/RSC
  // config, and confirmed the built dist/ bundle contains zero react-server
  // references. This is a plain client-side SPA (BrowserRouter, no data routers,
  // no framework/RSC mode) and never loads the code path the advisory targets.
  //
  // Re-check this exception if the app ever adopts SSR/RSC, or once react-router
  // ships a fix that doesn't require a major bump (currently only resolved at
  // react-router 8.3.0+, which has not been evaluated for this app).
  "high": true,
  "allowlist": ["GHSA-qwww-vcr4-c8h2"],
  "skip-dev": true
}
